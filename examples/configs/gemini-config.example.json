{
  "_comment": "Gemini CLI MCP config — Location: ~/.gemini/settings.json",
  "mcpServers": {
    "Common-test-MCP": {
      "command": "uvx",
      "args": ["Common-test-MCP"],
      "env": {
        "QA_RUNNER": "pytest",
        "QA_PROJECT_ROOT": "/absolute/path/to/your-test-project"
      }
    }
  }
}
